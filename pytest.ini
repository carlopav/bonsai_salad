[pytest]
testpaths = tests
; the repo root is a Blender addon package (bpy/bonsai imports): importlib
; mode keeps pytest from importing its __init__.py during collection
addopts = --import-mode=importlib
