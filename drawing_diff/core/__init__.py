"""drawing_diff.core -- pure Python core of the demolitions/additions comparison.

lxml, shapely, ezdxf, numpy and ifcopenshell only: no bpy, no bonsai, so the
whole pipeline (SVG parsing, booleans, SVG/DXF output, IFC provenance) is
testable outside Blender.
"""
